# Paths prettier must never format: build output, the OpenAPI document
# `sharerr openapi` generates, and untracked agent notes and worktrees.
target/
docs/openapi.json
CLAUDE.local.md
.claude/worktrees/
